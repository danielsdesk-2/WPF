<Window x:Class="ExcelLikeGrid.Sandbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ExcelLikeGrid.UI.Controls;assembly=ExcelLikeGrid.UI"
        mc:Ignorable="d"
        Title="ExcelLikeGrid Sandbox"
        Height="500"
        Width="900">

    <Grid Margin="16">
        <controls:ExcelLikeDataGrid
            x:Name="TestGrid"
            ItemsSource="{Binding Rows}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="True">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    SortMemberPath="Name" />

                        <DataGridTextColumn Header="Category"
                                    Binding="{Binding Category}"
                                    SortMemberPath="Category" />

                        <DataGridTextColumn Header="Amount"
                                    Binding="{Binding Amount}"
                                    SortMemberPath="Amount" />

                        <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}"
                                    SortMemberPath="Status" />
                    </DataGrid.Columns>
                </controls:ExcelLikeDataGrid>
    </Grid>
</Window>