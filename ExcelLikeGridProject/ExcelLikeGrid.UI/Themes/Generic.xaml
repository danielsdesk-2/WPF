<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ExcelLikeGrid.UI.Controls"
    xmlns:helpers="clr-namespace:ExcelLikeGrid.UI.Helpers">

    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

    <Geometry x:Key="FilterIconGeometry">
        F1 M 1,1 L 11,1 7,5 7,10 5,9 5,5 Z
    </Geometry>

    <Style x:Key="ExcelColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="WhiteSmoke" />
        <Setter Property="BorderBrush" Value="#D0D0D0" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="8,4,8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0"
                                              Margin="6,0,4,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left" />

                            <Path Grid.Column="1"
                                  Width="10"
                                  Height="10"
                                  Margin="0,0,6,0"
                                  VerticalAlignment="Center"
                                  Fill="DimGray"
                                  Data="{StaticResource FilterIconGeometry}"
                                  Visibility="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                       Path=Column.(helpers:ColumnFilterProperties.HasActiveFilter),
                                                       Converter={StaticResource BoolToVisibilityConverter}}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:ExcelLikeDataGrid}"
           BasedOn="{StaticResource {x:Type DataGrid}}">
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource ExcelColumnHeaderStyle}" />
    </Style>

</ResourceDictionary>